require 'spec_helper'

describe Dotenv::Beefy do
  it 'has a version number' do
    expect(Dotenv::Beefy::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
